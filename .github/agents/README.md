# Agents

This repository currently uses instruction-driven behavior via `.github/copilot-instructions.md`.

If custom Copilot agents are added later:

- Keep each agent narrowly scoped.
- Reference SSOT files instead of duplicating policy text.
- Include usage examples and validation expectations.
